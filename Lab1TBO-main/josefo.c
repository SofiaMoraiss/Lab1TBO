#include <stdio.h>
#include <stdlib.h>
#include "DoubleLinkedList.h"


int main (){

    int N;
    scanf ("%d", &N);
    no * head;

    InicializaLista(N, head);

    ImprimeLista(N, head);

    return 0;
}